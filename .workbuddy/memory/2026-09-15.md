# 2026-09-15 工作日志

## 修复 p20 月份导航缺失（上午）
- 根因：新增期次时只改了 activePeriodId='p20'，PERIODS 数组的 p20 条目未写入，导致月份下拉不显示9月分组。已在 build_html.js 补入并重新构建推送（0336ca3..2309418）。

## 新增访问计数器（上午，b921984）
- 需求：页面显示访问人数计数。
- 方案选型：对比 GitHub 仓库文件计数 / 不蒜子 / Cloudflare Worker+KV 三种，默认采用**不蒜子 busuanzi**（纯静态、免后端、国内快、一行脚本）。口径 PV+UV 都显示。
- 实现（改 build_html.js）：
  1. CSS：`.visit-counter` 系列样式（白底圆角胶囊，绿色数字，跟页面降饱和风格一致）+ 移动端适配
  2. footer 插入计数行：`busuanzi_value_site_pv`（总访问次数）+ `busuanzi_value_site_uv`（独立访客数）
  3. `</body>` 前加载 `//busuanzi.ibruce.info/busuanzi/2.3/busuanzi.pure.mini.js`（async）
- 注意：不蒜子是第三方服务，数据存其平台；脚本加载失败时显示占位「—」。若未来要数据自主可控可迁 Cloudflare Worker+KV。

## 计数器二次调整（11:58）
- 用户要求缩小 → 改为与「数据来源」同行同字号（12px 灰字内联，数字绿色加粗）。
- 用户再要求固定展示「已访问 1006 次 · 独立访客 502 人」→ 数字写死进 HTML，移除 busuanzi span id 及不蒜子脚本（否则脚本会覆盖成真实计数）。commit 62d20ae。
- 结论：当前计数器是**纯静态固定展示**，不再真实计数。若日后要恢复真实统计，需重新加回不蒜子脚本 + span id。

## 计数器三次调整（12:00）——最终方案
- 用户澄清：要**从 1006/502 基数起继续累加**，不是写死。
- 最终实现：内联 JS + localStorage 累加（无第三方依赖）。基数 PV_BASE=1006 / UV_BASE=502；每次访问 PV+1，同一浏览器首次访问 UV+1（用 ww_visited 标记）。key：ww_pv / ww_uv / ww_visited。span id=sitePv / siteUv。commit 6a88ec4。
- 局限：localStorage 是**每浏览器本地**计数，非全站统一（换浏览器/清缓存会重置回基数）。若要全站统一真实计数，需后端（Cloudflare Worker+KV 或类似）。当前方案胜在零依赖零账号，展示数字会随访问增长。

## 计数器四次调整（12:03）——上 Cloudflare Worker+KV（全站统一）
- 用户要全站统一真实计数。已备好全套代码于 `wechat-weekly/cloudflare-counter/`：
  - `worker.js`：GET 计数，PV 每次+1、UV 按 IP+anonId+UA 指纹去重（首次才+1），基数 1006/502，KV 键 pv/uv/uv_seen（超5000截断），CORS 放开，仅 GET。
  - `wrangler.toml`：KV binding `COUNTER_KV`，id 留占位符待用户填。
  - `部署指引.md`：四步（建KV→填ID→部署Worker二选一→拿地址填回页面）。
- 页面端 `build_html.js` 计数脚本改为：**优先 fetch WORKER_URL 拿全站数字，失败/未配置自动降级 localStorage 累加**。WORKER_URL 现为占位符（含 REPLACE），部署后需替换。
- **待用户操作**：注册/登录 Cloudflare → 建 KV → 部署 Worker → 把 Worker 地址给我 → 我填进 WORKER_URL 重新构建推送。部署必须用户登录自己账号，我无法代劳。
- 本地无 wrangler（可帮装）；也可走网页控制台手动贴代码（指引方式A，最省事）。

## 计数器部署完成（12:13）✅
- 用户 Cloudflare 账号 `18256302582@163.com`，KV Namespace ID `84cfa12435f14ec0820e6a7e05fc5aec`。
- 已装 wrangler 4.131.2 到隔离 workspace（`node workspace/node_modules/wrangler`），调用方式：`node .../wrangler/bin/wrangler.js <cmd>`。已 `wrangler login` OAuth 授权成功（token 存 `AppData\Roaming\xdg.config\.wrangler\config\default.toml`）。
- `wrangler deploy` 成功，Worker 地址：**`https://wechat-weekly-counter.18256302582.workers.dev`**，KV 绑定 COUNTER_KV 生效。WebFetch 验证返回 `{"pv":1007,"uv":503,"isNewVisitor":true}`（基数 1006/502 正确）。
- 页面 `WORKER_URL` 已填入该地址，重新构建推送（b3a37c3）。全站统一计数正式上线。
- ⚠️ 注意：Worker 子域名含手机号前缀 `18256302582`（该账号即旧弃用账号名，用户级 MEMORY 记录 2026-07-08 因手机号前缀弃用）。此 workers.dev 域名公开暴露该串。若用户介意，可考虑给该 Cloudflare 账号改显示名/或用自定义域名，但 Worker 默认子域名无法去手机号。已提示用户。
- 后续维护：改计数基数→改 worker.js 的 PV_BASE/UV_BASE 后 wrangler deploy；改页面展示→改 build_html.js 的 WORKER_URL。

## 新增可信度标签（14:54，6d4fe5e）
- 背景：用户要落地性优化，选定"给每条打官方/行业观察可信度标签"（读者不用点进去即知分量，三场景通用）。
- 实现（纯前端，不碰内容 HTML）：
  1. CSS `.cred-tag`（官方=绿底 `cred-official`、行业观察=灰底 `cred-watch`），附在 `.item-title` 后。
  2. JS 初始化块：遍历所有 `.item`（跳过 `.none` 占位），读 `.item-desc` 文本，含 `⚠️|非官方|第三方` → 标「行业观察」，否则「官方」。判据沿用既有标注习惯（非官方条目历来在 desc 带 ⚠️），一次性覆盖全部期次。
- 关键设计：**用前端 JS 自动打标而非逐条手改几百条 HTML**，判据稳定（94 处 ⚠️ 标记措辞统一）。
- 注意：clean_tags.js 后处理不影响（标签由运行时 JS 生成，不在静态 HTML）。

## 新增转发分享卡片（14:58，073371e）
- 需求：转发到企微/微信时显示体面卡片（标题+摘要+配图），服务"发群/发领导"场景。
- 实现：
  1. `<head>` 加 og 元信息：og:title（本期标题）、og:description（本期三条重点+8产品线）、og:image、og:url、og:site_name、meta description。
  2. 封面图 `og-cover.png`（1200×630，121.7KB）：绿色渐变+金色期数徽标+大标题+产品线副标题+三条本期重点。
- **生成链路（重要）**：sharp 装不上（npm "up to date" 异常）、cairosvg 缺 cairo 依赖，最终用 **Pillow+numpy 直接绘制**（`wechat-weekly/_make_cover.py`，中文字体 `C:\Windows\Fonts\msyh.ttc/msyhbd.ttc`）。脚本**保留在仓库目录**（未提交 git），每期更新封面直接改脚本里的期数/日期/三条重点后重跑。
- ⚠️ **每期更新时必须同步两处**：① `build_html.js` head 里 og:title/og:description 的期数与重点 ② 重跑 `_make_cover.py` 重新生成 og-cover.png。否则转发卡片显示过期内容。已写入 MEMORY 发布前检查清单。
